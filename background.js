class Background {
    draw() {
        image(game.backgroundImageBeach, 0, 0, width, height)
        image(game.backgroundImageShadow, 625, 590)
        image(game.backgroundImageNet, 625, 300,)
    }
}